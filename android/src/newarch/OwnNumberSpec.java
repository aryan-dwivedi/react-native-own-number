package com.ownnumber;

import com.facebook.react.bridge.ReactApplicationContext;

abstract class OwnNumberSpec extends NativeOwnNumberSpec {
  OwnNumberSpec(ReactApplicationContext context) {
    super(context);
  }
}
